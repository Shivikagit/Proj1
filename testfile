pipeline {
       agent any
       stages {
            stage ("Shell execute") {
                steps {
                    sh 'ls -l'
                    touch test.result
                      }
                }
               }
            }

