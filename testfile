pipeline {
       agent any
       stages {
            stage ("Shell execute") {
                steps {
                    sh 'ls -l'
                    sh 'touch test.result'
                }
            }
       }
 }

